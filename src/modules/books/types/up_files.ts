import { MulterFile } from "src/common/utils/multer.util";

export type BookImagesType = {
	media1: MulterFile[];
	media2: MulterFile[];
	media3: MulterFile[];
	media4: MulterFile[];
	media5: MulterFile[];
};
